/**
 * @description This callback redefine all arguments.
 * @callback TFnFingerprint
 * @param {import('express-fingerprint').FingerprintNext} cp
 * @param {import('express').Request} [req]
 * @param {import('express').Response} [res]
 */

/**
 * @description Middleware function
 * @callback TFnMiddleware
 * @param {import('express').Request} [req]
 * @param {import('express').Response} [res]
 * @param {import('express').NextFunction} [next]
 */

/**
 * @callback TFnHandler
 * @param {Object} [req]
 * @param {Object} [res]
 * @param {Function} [next]
 */

module.exports = {};